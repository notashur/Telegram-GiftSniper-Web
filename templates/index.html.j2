<!DOCTYPE html>
<html lang="en">

    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Gift Sniper Dashboard</title>
        <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
        <style>
            #bot-runtime {
                font-family: 'Courier New', monospace;
                letter-spacing: 1px;
            }

            .dark-mode #bot-runtime {
                color: #9ae6b4;
            }

            .notification-container {
                position: fixed;
                top: 1rem;
                right: 1rem;
                width: 320px;
                max-width: 90%;
                z-index: 1000;
            }

            .notification {
                position: relative;
                padding: 1rem;
                margin-bottom: 1rem;
                border-radius: 0.375rem;
                box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
                display: flex;
                align-items: center;
                animation: slideIn 0.3s ease-out forwards;
                opacity: 0;
                transform: translateX(100%);
            }

            .notification.success {
                background-color: #10B981;
                color: white;
            }

            .notification.error {
                background-color: #EF4444;
                color: white;
            }

            .notification.warning {
                background-color: #F59E0B;
                color: white;
            }

            .notification.info {
                background-color: #3B82F6;
                color: white;
            }

            .notification-icon {
                margin-right: 0.75rem;
                font-size: 1.25rem;
            }

            .notification-close {
                margin-left: auto;
                cursor: pointer;
                opacity: 0.7;
                transition: opacity 0.2s;
            }

            .notification-close:hover {
                opacity: 1;
            }

            @keyframes slideIn {
                to {
                    opacity: 1;
                    transform: translateX(0);
                }
            }

            @keyframes fadeOut {
                to {
                    opacity: 0;
                    transform: translateX(100%);
                }
            }

            .dark-mode {
                background-color: #1a202c;
                color: #e2e8f0;
            }

            .dark-mode .bg-white {
                background-color: #2d3748;
            }

            .dark-mode .text-gray-800 {
                color: #e2e8f0;
            }

            .dark-mode .bg-gray-50 {
                background-color: #4a5568;
            }

            .dark-mode .text-gray-700 {
                color: #cbd5e0;
            }

            .dark-mode .border {
                border-color: #4a5568;
            }

            .dark-mode input,
            .dark-mode select {
                background-color: #2d3748;
                color: #e2e8f0;
                border-color: #4a5568;
            }

            .dark-mode .bg-blue-500:hover {
                background-color: #3182ce;
            }

            .dark-mode .bg-green-500:hover {
                background-color: #38a169;
            }

            .dark-mode .bg-purple-500:hover {
                background-color: #805ad5;
            }

            .dark-mode .bg-red-500:hover {
                background-color: #e53e3e;
            }

            .dark-mode .text-blue-700 {
                color: #90cdf4;
            }

            .dark-mode .text-green-700 {
                color: #9ae6b4;
            }

            .dark-mode .text-purple-800 {
                color: #b794f4;
            }

            .dark-mode .text-green-800 {
                color: #9ae6b4;
            }

            .log-success {
                color: #4ade80;
            }

            .log-error {
                color: #f87171;
            }

            .log-info {
                color: #60a5fa;
            }

            .log-warning {
                color: #fbbf24;
            }

            .quick-logs {
                max-height: 300px;
                overflow-y: auto;
                scrollbar-width: thin;
                scrollbar-color: #4b5563 #1f2937;
                /* thumb and track color */
            }

            .quick-logs::-webkit-scrollbar {
                width: 8px;
            }

            .quick-logs::-webkit-scrollbar-track {
                background: #1f2937;
                border-radius: 4px;
            }

            .quick-logs::-webkit-scrollbar-thumb {
                background-color: #4b5563;
                border-radius: 4px;
                border: 2px solid #1f2937;
            }

            #pagination-controls button:not(:disabled):hover {
                background-color: #4b5563;
                transform: translateY(-1px);
            }

            #pagination-controls {
                transition: all 0.2s ease;
            }

            /* New enhancements */
            .stat-card {
                transition: transform 0.2s, box-shadow 0.2s;
                min-height: 120px;
                display: flex;
                flex-direction: column;
                justify-content: space-between;
            }

            .stat-card:hover {
                transform: translateY(-2px);
                box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.2);
            }

            .stat-title {
                font-size: 0.9rem;
                opacity: 0.8;
                margin-bottom: 0.5rem;
            }

            .stat-value {
                font-size: 1.5rem;
                font-weight: 700;
            }

            .section-title {
                position: relative;
                padding-bottom: 0.5rem;
                margin-bottom: 1.5rem;
            }

            .section-title:after {
                content: '';
                position: absolute;
                bottom: 0;
                left: 0;
                width: 50px;
                height: 3px;
                background: linear-gradient(90deg, #4f46e5, #10b981);
                border-radius: 3px;
            }

            .avoid-list {
                max-height: 100px;
                overflow-y: auto;
                padding-right: 0.5rem;
            }

            .avoid-list::-webkit-scrollbar {
                width: 6px;
            }

            .avoid-list::-webkit-scrollbar-track {
                background: #374151;
                /* bg-gray-700 */
                border-radius: 3px;
            }

            .avoid-list::-webkit-scrollbar-thumb {
                background-color: #6B7280;
                /* bg-gray-500 */
                border-radius: 3px;
            }

            .avoid-list::-webkit-scrollbar-thumb:hover {
                background-color: #9CA3AF;
                /* bg-gray-400 */
            }

            .avoid-list li {
                margin-bottom: 0.25rem;
                padding-left: 0.5rem;
            }

            /* Gifts to Avoid list styling */
            .avoid-list ul {
                list-style: none;
                padding-left: 0;
            }

            .avoid-list li {
                display: flex;
                align-items: center;
                padding: 2px 0;
            }

            .stat-card {
                display: flex;
                flex-direction: column;
                justify-content: flex-start;
                align-items: flex-start;
            }

            .avoid-list {
                align-self: stretch;
                margin-top: 0;
            }
        </style>
        <style>
            .image-container {
                position: relative;
                width: 40px;
                height: 40px;
                margin-right: 1rem;
            }

            .image-placeholder {
                position: absolute;
                top: 0;
                left: 0;
                width: 100%;
                height: 100%;
                display: flex;
                align-items: center;
                justify-content: center;
                background-color: rgba(255, 255, 255, 0.05);
                border-radius: 0.25rem;
            }

            .gift-image {
                position: absolute;
                top: 0;
                left: 0;
                width: 100%;
                height: 100%;
                object-fit: contain;
                border-radius: 0.25rem;
                opacity: 0;
                transition: opacity 0.3s ease;
            }

            .gift-image.loaded {
                opacity: 1;
            }

            .fa-spinner {
                animation: spin 1s linear infinite;
                color: #9CA3AF;
            }

            @keyframes spin {
                0% {
                    transform: rotate(0deg);
                }

                100% {
                    transform: rotate(360deg);
                }
            }
        </style>
        <style>
            .btn-loading {
                position: relative;
                pointer-events: none;
            }

            .btn-loading::after {
                content: '';
                position: absolute;
                top: 50%;
                left: 50%;
                width: 20px;
                height: 20px;
                margin: -10px 0 0 -10px;
                border: 2px solid rgba(255, 255, 255, 0.3);
                border-radius: 50%;
                border-top-color: white;
                animation: btn-spin 0.6s linear infinite;
            }

            @keyframes btn-spin {
                to {
                    transform: rotate(360deg);
                }
            }
        </style>
    </head>

    <body class="dark-mode bg-gray-900 min-h-screen">
        <div class="container mx-auto px-4 py-8">
            <!-- Header Section -->
            <div class="bg-gray-800 rounded-xl shadow-lg p-6 mb-8">
                <div class="flex flex-col md:flex-row justify-between items-start md:items-center gap-4">
                    <div>
                        <h1 class="text-3xl font-bold text-white mb-1">Gift Sniper Dashboard</h1>
                        <div class="flex items-center space-x-2">
                            <span class="text-sm text-gray-400">Status:</span>
                            <span id="bot-status-text"
                                class="text-sm font-medium {% if bot_status %}text-green-400{% else %}text-red-400{% endif %}">
                                {% if bot_status %}Running{% else %}Stopped{% endif %}
                            </span>
                        </div>
                    </div>

                    <div class="flex flex-wrap gap-2 w-full md:w-auto">
                        <button data-bot-status="{{ 'running' if bot_status else 'stopped' }}"
                            onclick="showConfirmDialog(this)"
                            class="px-4 py-2 rounded-lg text-white font-medium flex items-center {% if bot_status
                        %}bg-red-600 hover:bg-red-700{% else %}bg-green-600 hover:bg-green-700{% endif %} transition-colors">
                            {% if bot_status %}
                            <i class="fas fa-stop mr-2"></i>Stop
                            {% else %}
                            <i class="fas fa-play mr-2"></i>Start
                            {% endif %}
                        </button>
                        <a href="/settings"
                            class="px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 flex items-center transition-colors">
                            <i class="fas fa-cog mr-2"></i>Settings
                        </a>
                        {% if is_admin %}
                        <a href="/admin/dashboard"
                            class="px-4 py-2 bg-yellow-600 text-white rounded-lg hover:bg-yellow-700 flex items-center transition-colors">
                            <i class="fas fa-user-shield mr-2"></i>Admin
                        </a>
                        {% endif %}
                        <form action="/logout" method="POST" class="inline">
                            <button type="submit"
                                class="px-4 py-2 bg-red-600 text-white rounded-lg hover:bg-red-700 flex items-center transition-colors">
                                <i class="fas fa-sign-out-alt mr-2"></i>Logout
                            </button>
                        </form>
                    </div>
                </div>
            </div>
            <div id="confirmDialog"
                class="fixed inset-0 z-50 flex items-center justify-center bg-black bg-opacity-50 hidden">
                <div class="bg-white dark-mode:bg-gray-800 p-6 rounded-xl shadow-xl w-full max-w-sm">
                    <h2 class="text-lg font-semibold mb-4 text-gray-800 dark-mode:text-gray-100" id="confirmTitle">
                        Confirm
                    </h2>
                    <p class="text-gray-700 dark-mode:text-gray-300" id="confirmMessage">Are you sure you want to
                        proceed?
                    </p>
                    <div class="mt-6 flex justify-end gap-3">
                        <button onclick="closeConfirmDialog()"
                            class="px-4 py-2 rounded-lg bg-gray-300 hover:bg-gray-400 text-gray-800 dark-mode:bg-gray-700 dark-mode:hover:bg-gray-600 dark-mode:text-white transition">Cancel</button>
                        <button id="confirmAction"
                            class="px-4 py-2 rounded-lg text-white font-semibold transition"></button>
                    </div>
                </div>
            </div>
            <!-- Main Stats Grid -->
            <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-8">
                <!-- Current Balance -->
                <div class="bg-gray-700 p-5 rounded-xl shadow-md stat-card">
                    <div>
                        <h3 class="stat-title text-gray-300">Current Balance</h3>
                        <p id="current-balance-stars" class="stat-value text-white">
                            {{ current_balance_stars }}⭐
                        </p>
                    </div>
                    <div class="text-green-400 text-sm">
                        <i class="fas fa-coins mr-1"></i> Available stars
                    </div>
                </div>
                <!-- TON Balance -->
                <div class="bg-gray-700 p-5 rounded-xl shadow-md stat-card">
                    <div>
                        <h3 class="stat-title text-gray-300">TON Balance</h3>
                        <p id="current-balance-ton" class="stat-value text-white">
                            {{ current_balance_ton }}💎
                        </p>
                    </div>
                    <div class="text-cyan-400 text-sm">
                        <i class="fas fa-gem mr-1"></i> Available TON
                    </div>
                </div>
                <!-- Bot Runtime -->
                <div class="bg-gray-700 p-5 rounded-xl shadow-md stat-card">
                    <div>
                        <h3 class="stat-title text-gray-300">Bot Runtime</h3>
                        <p id="bot-runtime" class="stat-value text-white">
                            00:00:00
                        </p>
                    </div>
                    <div class="text-blue-400 text-sm">
                        <i class="fas fa-clock mr-1"></i> Active time
                    </div>
                </div>

                <!-- Subscription Expiration -->
                {% if expiry_timestamp %}
                <div id="subscription-info" data-expiry="{{ expiry_timestamp }}" style="display:none;"></div>
                <div class="bg-gray-700 p-5 rounded-xl shadow-md stat-card">
                    <div>
                        <h3 class="stat-title text-gray-300">Subscription Expiration</h3>
                        <p id="countdown" class="stat-value text-white">
                            <span id="expiry-date" class="block text-sm font-normal text-gray-300 mb-1">{{ expiry_date
                                }}</span>
                            <span id="countdown-timer" class="text-xl">Loading...</span>
                        </p>
                    </div>
                    <div class="text-purple-400 text-sm">
                        <i class="fas fa-calendar-alt mr-1"></i> Renewal date
                    </div>
                </div>
                {% else %}
                <div id="subscription-info" style="display:none;">No subscription</div>
                <div class="bg-gray-700 p-5 rounded-xl shadow-md stat-card">
                    <div>
                        <h3 class="stat-title text-gray-300">Subscription Expiration</h3>
                        <p class="stat-value text-red-400">No subscription</p>
                    </div>
                    <div class="text-purple-400 text-sm">
                        <i class="fas fa-calendar-alt mr-1"></i> Renewal date
                    </div>
                </div>
                {% endif %}
            </div>

            <!-- Quick Stats Section -->
            <div class="bg-gray-800 rounded-xl shadow-lg p-6 mb-8">
                <h2 class="section-title text-xl font-semibold text-white">Quick Stats</h2>

                <div class="grid grid-cols-1 md:grid-cols-4 gap-4">
                    <!-- Auto-Buy Limit -->
                    <!-- Gift Star Limits -->
                    <!-- Gift Star Limits -->
                    <div class="bg-gray-700 p-4 rounded-lg stat-card">
                        <div class="flex items-center mb-2">
                            <i class="fas fa-star text-yellow-400 mr-2"></i>
                            <h3 class="font-medium text-yellow-400">Gift Star Limits</h3>
                        </div>
                        {% if settings.GIFT_LIMITS %}
                        <div class="avoid-list">
                            <ul class="text-sm text-white space-y-2">
                                {% for gift_name, limit in settings.GIFT_LIMITS.items() %}
                                {% set gift_id = GIFT_MAPPINGS|find_key(gift_name) %}
                                <li class="flex items-center justify-between">
                                    <div class="flex items-center">
                                        {% if gift_id %}
                                        <div class="relative w-6 h-6 mr-2">
                                            <div
                                                class="image-placeholder w-full h-full flex items-center justify-center">
                                                <i class="fas fa-spinner fa-spin text-gray-400 text-xs"></i>
                                            </div>
                                            <img src="{{ url_for('static', filename='gifts/' ~ gift_id ~ '.png') }}"
                                                alt="{{ gift_name }}"
                                                class="absolute inset-0 w-full h-full object-contain opacity-0 transition-opacity duration-300"
                                                onload="this.classList.remove('opacity-0'); this.previousElementSibling.style.display = 'none';"
                                                onerror="this.style.display='none'; this.previousElementSibling.innerHTML='<i class=\'fas fa-image text-gray-400 text-xs\'></i>';">
                                        </div>
                                        {% endif %}
                                        <span>{{ gift_name }}</span>
                                    </div>
                                    <span class="bg-gray-600 px-2 py-1 rounded text-xs font-medium">{{ limit }}⭐</span>
                                </li>
                                {% endfor %}
                            </ul>
                        </div>
                        {% else %}
                        <p class="text-gray-400 italic text-sm">Using default limit ({{
                            settings.DEFAULT_GIFTS_TO_BUY_MAX_PRICE }}⭐) for all gifts</p>
                        {% endif %}
                    </div>

                    <!-- Scan Interval -->
                    <div class="bg-gray-700 p-4 rounded-lg stat-card">
                        <div class="flex items-center mb-2">
                            <i class="fas fa-sync-alt text-purple-400 mr-2"></i>
                            <h3 class="font-medium text-purple-400">Scan Interval</h3>
                        </div>
                        <p class="text-xl font-bold text-white">{{ settings.SLEEP_BETWEEN_CYCLES }}s</p>
                        <p class="text-xs text-gray-400 mt-1">Between checks</p>
                    </div>

                    <!-- Gifts to Avoid -->
                    <div class="bg-gray-700 p-4 rounded-lg stat-card">
                        <div class="flex items-center mb-2">
                            <i class="fas fa-ban text-red-400 mr-2"></i>
                            <h3 class="font-medium text-red-400">Gifts to Avoid</h3>
                        </div>
                        {% if settings.GIFTS_NOT_TO_BUY %}
                        <div class="avoid-list">
                            <ul class="text-sm text-white space-y-2">
                                {% for gift in settings.GIFTS_NOT_TO_BUY %}
                                {% set gift_id = GIFT_MAPPINGS|find_key(gift) %}
                                <li class="flex items-center">
                                    {% if gift_id %}
                                    <div class="relative w-6 h-6 mr-2">
                                        <div class="image-placeholder w-full h-full flex items-center justify-center">
                                            <i class="fas fa-spinner fa-spin text-gray-400 text-xs"></i>
                                        </div>
                                        <img src="{{ url_for('static', filename='gifts/' ~ gift_id ~ '.png') }}"
                                            alt="{{ gift }}"
                                            class="absolute inset-0 w-full h-full object-contain opacity-0 transition-opacity duration-300"
                                            onload="this.classList.remove('opacity-0'); this.previousElementSibling.style.display = 'none';"
                                            onerror="this.style.display='none'; this.previousElementSibling.innerHTML='<i class=\'fas fa-image text-gray-400 text-xs\'></i>';">
                                    </div>
                                    {% endif %}
                                    <span>{{ gift }}</span>
                                </li>
                                {% endfor %}
                            </ul>
                        </div>
                        {% else %}
                        <p class="text-gray-400 italic text-sm">No gifts selected to avoid</p>
                        {% endif %}
                    </div>

                    <!-- Backdrops to Avoid -->
                    <!-- Backdrops to Avoid -->
                    <div class="bg-gray-700 p-4 rounded-lg stat-card flex flex-col"> <!-- Add flex flex-col here -->
                        <div class="flex items-center mb-2">
                            <i class="fas fa-image text-yellow-400 mr-2"></i>
                            <h3 class="font-medium text-yellow-400">Backdrops to Avoid</h3>
                        </div>
                        {% if settings.BACKDROPS_NOT_TO_BUY %}
                        <div class="avoid-list max-h-40 overflow-y-auto pr-2 mt-0">
                            <!-- Add mt-0 to remove any default margin -->
                            <ul class="text-sm text-white">
                                {% for backdrop in settings.BACKDROPS_NOT_TO_BUY %}
                                <li class="flex items-center py-1"> <!-- Add py-1 for consistent padding -->
                                    {% if backdrop in BACKDROP_CENTER_COLORS %}
                                    <span class="w-4 h-4 rounded-full mr-2 border border-gray-400/30"
                                        style="background-color: {{ BACKDROP_CENTER_COLORS[backdrop] }}"></span>
                                    {% endif %}
                                    <span>{{ backdrop }}</span>
                                </li>
                                {% endfor %}
                            </ul>
                        </div>
                        {% else %}
                        <p class="text-gray-400 italic text-sm">No backdrops selected to avoid</p>
                        {% endif %}
                    </div>
                </div>
            </div>

            <!-- Quick Logs Section -->
            <div class="bg-gray-800 rounded-xl shadow-lg p-6">
                <h2 class="section-title text-xl font-semibold text-white">Recent Activity</h2>

                <div class="bg-gray-900 rounded-lg p-4 h-64 overflow-y-auto quick-logs">
                    <ul id="quick-logs-content" class="space-y-2 font-mono text-sm">
                        <!-- Logs will appear here -->
                        <li class="text-gray-400 italic">Loading activity logs...</li>
                    </ul>
                </div>

                <!-- Pagination Controls -->
                <div id="pagination-controls" class="flex justify-between items-center mt-3 hidden">
                    <button id="prev-page"
                        class="hidden px-3 py-1 bg-gray-700 text-gray-300 rounded hover:bg-gray-600 transition-colors">
                        ← Previous
                    </button>
                    <span id="page-info" class="text-gray-400 text-sm"></span>
                    <button id="next-page"
                        class="hidden px-3 py-1 bg-gray-700 text-gray-300 rounded hover:bg-gray-600 transition-colors">
                        Next →
                    </button>
                </div>
            </div>
        </div>
        <!-- Display Flash Messages -->
        <div id="notification-container" class="notification-container"> {% with messages =
            get_flashed_messages(with_categories=true) %}
            {% if messages %}
            {% for category, message in messages %}
            <div class="notification {{ category }} mb-3">
                <i class="notification-icon 
                            {% if category == 'success' %}fas fa-check-circle
                            {% elif category == 'error' %}fas fa-exclamation-circle
                            {% elif category == 'warning' %}fas fa-exclamation-triangle
                            {% else %}fas fa-info-circle{% endif %}"></i>
                <span>{{ message }}</span>
                <span class="notification-close" onclick="this.parentElement.remove()">
                    <i class="fas fa-times"></i>
                </span>
            </div>
            {% endfor %}
            {% endif %}
            {% endwith %}
        </div>
        <script src="{{ url_for('static', filename='js/index.js') }}"></script>
    </body>

</html>